export const metadata = {
  title: "Sign up — TripSheetHQ",
  description: "Create your TripSheetHQ account or join your company to start logging trips, tracking fuel, and managing dispatch messages.",
};

export default function SignupLayout({ children }) {
  return children;
}