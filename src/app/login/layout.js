export const metadata = {
  title: "Log in — TripSheetHQ",
  description: "Log in to TripSheetHQ to access your trip sheets, dispatch messages, and ACE/ACI documents.",
};

export default function LoginLayout({ children }) {
  return children;
}